function emerMessage() {// me creo la funcion que se va a realizar
    alert("Holaaa.")    //cuando pase el tiempo que le introducimos.
    console.log()
}
//creamos una constante mensaje que almacena el setTimeOut.
const MENSAJE= setTimeout(emerMessage,3000);//Con esta funcion le pasamos por parametros el tiempo que va a transcurrir hasta que se
                                                   //ejecute la funcion que le pasamos por parametro.